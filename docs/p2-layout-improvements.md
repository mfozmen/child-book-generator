# p2 — Layout improvements

Placeholder. Not started until Phase 1 (PDF-first ingestion) is complete.

Likely work items (will be split into discrete `p2-*.md` files when we get there):

- Smarter per-page layout selection (based on image aspect ratio + text length) instead of a fixed layout.
- Richer layouts beyond the current four (`image-top`, `image-bottom`, `image-full`, `text-only`) — e.g. two-image spreads, caption boxes, decorative frames.
- Typography pass: line spacing, hyphenation, widow/orphan control for children's books.
- Cover / back-cover templates (currently ad-hoc).
- Per-page background color / tint support.

This file is intentionally short until Phase 1 is done.
