# docs/

Internal planning notes.

The current roadmap lives in [`PLAN.md`](PLAN.md) — an agent-first pivot that supersedes the old phase files (`p*.md` deleted on 2026-04-14). When PRs in `PLAN.md` ship, trim the corresponding sections; when the file is empty, the plan is done.

Nothing in this folder is user-facing. User docs live in the root `README.md`.
